<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blockchain Quiz</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background-image: url('../img/blockchain-background.jpg');
            background-size: cover;
            background-position: center;
        }
        .container {
            background-color: rgba(255, 255, 255, 0.9);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.3);
            text-align: center;
            max-width: 500px;
        }
        .question {
            font-size: 1.5em;
            margin-bottom: 20px;
            color: #333;
        }
        .options {
            list-style: none;
            padding: 0;
        }
        .options li {
            margin: 10px 0;
        }
        .options input {
            margin-right: 10px;
        }
        #nextButton {
            margin-top: 20px;
            padding: 10px 20px;
            font-size: 1em;
            cursor: pointer;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
        }
        #nextButton:disabled {
            background-color: #ccc;
            cursor: not-allowed;
        }
    </style>
</head>
<body>

<div class="container">
    <div id="quiz">
        <div class="question" id="question">Question 1: What is a blockchain?</div>
        <ul class="options">
            <li><input type="radio" name="answer" value="0">A chain of blocks containing data</li>
            <li><input type="radio" name="answer" value="1">A centralized database</li>
            <li><input type="radio" name="answer" value="2">A type of cryptocurrency</li>
            <li><input type="radio" name="answer" value="3">A social network</li>
        </ul>
        <button id="nextButton" disabled>Next</button>
    </div>
</div>

<script>
    username=prompt("Insert your username");
    const quizData = [
        {
            question: "Question 1: What is a blockchain?",
            options: [
                "A chain of blocks containing data",
                "A centralized database",
                "A type of cryptocurrency",
                "A social network"
            ],
            correct: 0
        },
        {
            question: "Question 2: Which of these is an example of a cryptocurrency based on blockchain?",
            options: ["Bitcoin", "Euro", "PayPal", "Libra"],
            correct: 0
        },
        {
            question: "Question 3: What is the main advantage of a blockchain?",
            options: [
                "Decentralization",
                "Increased privacy",
                "Interoperability",
                "Transaction speed"
            ],
            correct: 0
        },
        {
            question: "Question 4: Who created Bitcoin?",
            options: [
                "Satoshi Nakamoto",
                "Vitalik Buterin",
                "Elon Musk",
                "Bill Gates"
            ],
            correct: 0
        }
    ];

    let currentQuestionIndex = 0;
    let correctAnswers = 0;
    const questionElement = document.getElementById('question');
    const optionsElement = document.querySelector('.options');
    const nextButton = document.getElementById('nextButton');

    function loadQuestion() {
        const currentQuestion = quizData[currentQuestionIndex];
        questionElement.textContent = currentQuestion.question;
        optionsElement.innerHTML = '';

        currentQuestion.options.forEach((option, index) => {
            const li = document.createElement('li');
            li.innerHTML = `<input type="radio" name="answer" value="${index}">${option}`;
            optionsElement.appendChild(li);
        });

        nextButton.disabled = true;
    }

    optionsElement.addEventListener('change', () => {
        nextButton.disabled = false;
    });

    nextButton.addEventListener('click', () => {
        const selectedOption = document.querySelector('input[name="answer"]:checked');
        if (selectedOption) {
            const answerValue = parseInt(selectedOption.value);
            if (answerValue === quizData[currentQuestionIndex].correct) {
                correctAnswers++;
            }

            currentQuestionIndex++;
            if (currentQuestionIndex < quizData.length) {
                loadQuestion();
            } else {
                if (correctAnswers === quizData.length) {
                    sendResult();
                } else {
                    questionElement.textContent = "You completed the quiz, but not all answers were correct.";
                    optionsElement.innerHTML = '';
                    nextButton.style.display = 'none';
                }
            }
        }
    });

    function sendResult() {
        const xhr = new XMLHttpRequest();
        xhr.open("POST", "/result", true);
        xhr.setRequestHeader("Content-Type", "application/json;charset=UTF-8");
        xhr.send(JSON.stringify({ score: correctAnswers, "username": username, quiz: "blockchain" }));
        
        questionElement.textContent = "Quiz completed with all correct answers! Result sent.";
        optionsElement.innerHTML = '';
        nextButton.style.display = 'none';
    }

    loadQuestion();
</script>

</body>
</html>